function getInput(fieldId) {
    const amount = document.getElementById(fieldId);
    const oldAmount = amount.innerText;
    const prevAmount = parseFloat(oldAmount);
};

document.getElementById('deposit-submit').addEventListener('click', function () {
    const inputDepositAmount = document.getElementById('deposit');

})
document.getElementById('withdraw-submit').addEventListener('click', function () {
    const inputWithdrawAmount = document.getElementById('withdraw-input');

})